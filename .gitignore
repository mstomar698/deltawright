node_modules/
dist/
test-results/
playwright-report/
.DS_Store
*.log

# Claude Code local working dir (agent worktrees / config) — never committed
.claude/

# Local env / secrets — NEVER commit API keys
.env
.env.*
!.env.example

# Live-benchmark targets point at real sites + credentials — keep them local
bench/live-targets.json
