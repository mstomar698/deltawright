node_modules/
dist/
test-results/
playwright-report/
.DS_Store
*.log

# Claude Code ephemeral agent worktrees — never committed. Scoped to worktrees/ so an intentionally
# committed .claude/settings.json (shared config) is still allowed.
.claude/worktrees/

# Local env / secrets — NEVER commit API keys
.env
.env.*
!.env.example

# Live-benchmark targets point at real sites + credentials — keep them local
bench/live-targets.json
