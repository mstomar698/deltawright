# review · v0.1-milestone · adversarial verification

Source: a 4-lens adversarial verification (correctness, actionability-alignment,
go/no-go honesty, scope-compliance) reading the real code, plus a diverse design
panel during Phase 0. Every finding gets a disposition line — no silent skips.

## summary
5 in-scope defects (3 high, 2 medium) · 3 in-scope nits (low) · several honest
out-of-scope caveats · constraints all upheld.
ready: yes — all in-scope high/medium defects fixed and regression-tested; go/no-go
is GO-with-caveats (see `docs/summaries/v0.1-milestone.md`).

## in-scope defects — dispositions
- [V1] high · actionability probe is click-only, so the verdict is click-actionability and unproven for the textbox Case 1 ships — **fixed (honesty):** verdict labeled pointer/click-actionability in `types.ts`/README; Case 1 now proves the textbox against a real `fill`. Role-aware probes deferred to v0.5 (DW-02) — test: `case 1 … fully ACTIONABLE`.
- [V2] high · net-removal of a pre-existing element that is also moved in-window is silently dropped (`!addedCand.has` conflated "moved" with "born") — **fixed:** record-order `firstTouch` in `coalesce` (`observer.ts`) — test: `a pre-existing element moved then removed still nets to removed`.
- [V3] high · geometry↔Playwright disagreement computed but never serialized; no `agreed===false` case existed — **fixed:** `[geom:…]` marker in `serialize.ts` + new disabled-button fixture case — test: `case 4 … disagreement surfaced`.
- [V4] medium · attr/text churn that reverts to the original value is a false positive — **fixed (attr):** net-compare earliest `oldValue` vs current in `coalesce` — test: `attribute churn that reverts to the original value is NOT reported`. Text net-compare deferred to v0.5 (documented).
- [V5] medium · `elementFromPoint` treated an ancestor painted over its descendant as `hitSelf` — **fixed:** ancestor-on-top now counts as covered, matching Playwright's "hit target must be self or descendant" (`observer.ts readGeometry`).
- [V9] low · `offscreen` derived from the element's center point mislabels a large viewport-straddling element — **accepted for v0.1:** final verdict stays correct (Playwright wins; disagreement surfaced). Documented limitation.
- [V10] low · nested removals could be double-reported un-nested (detached descendant has null `parentElement`) — **accepted for v0.1:** a removed subtree reports only its root in `removedNodes`, so the normal case is single-node; noted.
- [V11] low · reason string is geometry-first and can misattribute when both sides say NOT-actionable for different causes — **accepted + documented:** `Actionability.reason` doc now states only `verdict` is Playwright-authoritative; the reason is a best-effort gloss.

## out-of-scope caveats — stated plainly, not fixed (v0.5)
- [V6] settle can return zero nodes when the change lands after `maxWaitMs` — **mitigated:** the serializer now renders `(no changes captured before maxWait cap — SUSPECTED MISS)` instead of an empty confident delta; robust settle is DW-01.
- [V7] trial timeout (1200ms) ≪ a real action's 30s budget, so a transiently-not-yet-actionable node may be reported NOT-actionable though the real action would wait it out — v0.5.
- [V8] verdict is a point-in-time probe separate from the agent's later action; animations on unchanged pre-existing elements aren't awaited (TOCTOU) — v0.5.
- Reconciliation is serial O(nodes) and each NOT-actionable node pays the full trial timeout — v0.5 (async/bounded).

## constraint compliance
Upheld: no browser/engine fork; actionability reused from Playwright (authoritative, wins); no shadow/iframe/canvas/CDP/MCP smuggled in; settle is a labeled tunable heuristic. No scope violations.

## plan trace
- A1–A7 → `src/injected/observer.ts`, `src/host/*.ts`, `test/*.spec.ts` ✓
- No orphan lines: every source file maps to a plan slice; the added removal/net-compare/disagreement tests trace to A3/A5.
